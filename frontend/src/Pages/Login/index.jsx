import Button from "../../components/Button/Button"

const Login = (props) => {
    return (
        <div>
            Hello from Login
            <Button title="login"/>
        </div>
    );
}

export default Login;